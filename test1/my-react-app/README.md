# test1 - Get Current Weather by City

Search component gets current weather condition and temperature by city via API and sends the data up the component tree (App.jsx) and down to Card.jsx via props.
